KeyValidationApp:

<KeyValidationApp>:
    major_scale: major_scale
    minor_scale: minor_scale
    modes: modes
    chords: chords
    GridLayout:
        canvas.before:
            Color:
                rgba:1, .5, .6, .5

            Rectangle:
                pos: self.pos
                size: self.size
        rows:5
        cols:5
        padding: 10
        spacing: 10
        CustomSpinner:
            text: 'keys'
            values: "C", "G", "D", "A", "E", "B", "F#", "C#","F","Bb","Eb","Ab","Db","Gb","Cb"
            size_hint: 1, 1
            size: 33, 33
            pos_hint: {'center_x': .9, 'center_y': .1}
            on_text: root.on_spinner_change(self.text)
        Label:
            text: "major scale"
        TextInput:
            id: major_scale
            multiline: True
        Label:
            text: "minor scale"
        TextInput:
            id: minor_scale
            multiline: True
        Label:
            text: "modes"
        TextInput:
            id: modes
            multiline: True
        Label:
            text: "chords"
        TextInput:
            id: chords
            multiline: True
        Button:
            text: 'Go to Next Page'
            on_press: print("Page2")
